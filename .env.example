# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# OpenAI (required — powers Router, Conversation, Builder, Reviewer agents)
OPENAI_API_KEY=

# Vercel (optional — only needed for the "Publish live" deploy button)
VERCEL_TOKEN=

# fal.ai (optional — only needed for /api/image visual generation)
FAL_KEY=
